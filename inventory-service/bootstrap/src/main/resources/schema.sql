CREATE TABLE IF NOT EXISTS operations(order_id UUID PRIMARY KEY,product_id VARCHAR(100) NOT NULL,quantity INTEGER NOT NULL,amount NUMERIC(19,2) NOT NULL,status VARCHAR(50) NOT NULL,updated_at TIMESTAMPTZ NOT NULL DEFAULT now());
