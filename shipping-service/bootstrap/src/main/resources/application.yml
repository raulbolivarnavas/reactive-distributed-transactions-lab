server:
  port: ${SERVER_PORT:8083}
spring:
  application:
    name: shipping-service
  r2dbc:
    url: ${DB_URL:r2dbc:postgresql://localhost:5432/shipping}
    username: ${DB_USER:sagalab}
    password: ${DB_PASSWORD:sagalab}
  sql:
    init:
      mode: always
management:
  endpoints:
    web:
      exposure:
        include: health,info
